apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: speech2text-frontend
spec:
  tls:
    termination: edge
  to:
    kind: Service
    name: speech2text-frontend
    weight: 100
  wildcardPolicy: None
